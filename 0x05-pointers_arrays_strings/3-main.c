#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

int main(void)
{
	char *haven;

	haven = "I do not fear computers. I fear the lack of them - Isaac Asimov";
	_puts(haven);
	return (0);
}
