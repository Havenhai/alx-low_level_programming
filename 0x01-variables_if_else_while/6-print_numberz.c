#include <stdio.h>

/**
 * main - Prints all single digit numbers of base 10 starting from 0.
 * Return: Always 0
 */
int main(void)
{
	int h;

	for (h = 48; h < 58; h++)
	{
		putchar(h);
	}
	putchar('\n');
	return (0);
}
