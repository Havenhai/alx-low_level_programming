#include "main.h"

/**
 * _isupper - Check for uppercase character and tell if the letter is uppercase
 *
 * @h: The number to be checked
 *
 * Return: 1  if h is uppercase and 0 otherwise
 */

int _isupper(int h)
{
	if (h >= 65 && x <= 90)
	{
		return (1);
	}
		return (0);
}
